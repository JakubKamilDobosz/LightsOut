<Window x:Class="PaylAllJakubTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PaylAllJakubTest"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid x:Name="MainGrid" Margin="36,10,31,51" Background="{DynamicResource {x:Static SystemColors.ActiveCaptionBrushKey}}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="col0" />
            <ColumnDefinition x:Name="col1" />
            <ColumnDefinition x:Name="col2" />
            <ColumnDefinition x:Name="col3" />
            <ColumnDefinition x:Name="col4" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition x:Name="row0"/>
            <RowDefinition x:Name="row1"/>
            <RowDefinition x:Name="row2"/>
            <RowDefinition x:Name="row3"/>
            <RowDefinition x:Name="row4"/>
        </Grid.RowDefinitions>
    </Grid>
</Window>
